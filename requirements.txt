# o7Debrief runtime dependencies.
# Install with:  pip install -r requirements.txt
# Requires Python 3.13 or newer (the app reads TOML with the standard-library
# tomllib, and uses only the standard library otherwise apart from the two below).

PySide6>=6.6,<7.0
Jinja2>=3.1,<4.0
