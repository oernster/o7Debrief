# o7Debrief development dependencies: tests, formatting, linting and the build.
# Installs the runtime dependencies too.
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt

# Tests and coverage (the suite enforces a 100% gate on domain + application).
pytest>=8
pytest-cov>=5

# Formatting and linting (line length 88). Both linters are configured and both
# are expected to be clean; see DEVELOPMENT-README.md for why ruff needs the
# configuration it carries.
black>=24
flake8>=7
ruff>=0.6

# Standalone Windows build (buildexe.py / buildinstaller.py).
Nuitka>=2.4

# Flatpak build (build_flatpak.sh): derives the hicolor icon set from the
# assets/o7Debrief.png master. Build-time only; the app never imports it.
Pillow>=10
